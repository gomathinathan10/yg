# Server & Network Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Application Base URL (used in SSR API calls: src/lib/api-client.ts)
APP_URL=http://localhost:3000

# Deployment & Build Configuration (node-server / vercel)
NITRO_PRESET=node-server

# Public Client-side Variables (prefixed with VITE_)
VITE_SITE_URL=https://ygasafoetida.in
VITE_APP_TITLE="Y.G Asafoetida"
